;; -*- eval: (flycheck-mode -1) -*-
(defvar t/boot-sequence (list
                         :hello "config"
                         :file frontmacs-config-file
                         :done (featurep 'frontmacs)))
